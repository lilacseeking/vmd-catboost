# 配电网物资需求预测系统 — 设计与方法论审计报告

审计日期: 2026-07-08
审计范围: 项目全部文档（CLAUDE.md、数据生成要求、实验报告）+ main.py 架构设计 + scripts/ 子模块
审计方法: 第一性原理逐层质疑，从问题定义到实验设计到文档一致性
与 AUDIT_REPORT.md 的关系: 该文件聚焦代码级 Bug（expm1/泄露/阈值），本报告聚焦设计层面和方法论层面的结构性问题

---

### 一、根本性问题：问题定义与方法论的自相矛盾

#### 发现 #1: 项目文档已明确承认当前方法不可行，但项目仍在此方向上持续投入

**证据**: CLAUDE.md 第 102 行明确写道：

> "**当前方法已确认不可行。** 全部模型（CatBoost, VMD-LSTM-CatBoost, Croston-TSB-LGB, LGB Direct Multi-Step）在5种物资上均无法稳定超越 Naive-Seasonal 基线。根因分析见 `bidding-ecp-data/reports/预测失败根因分析与改进方案.md`——74个月中仅27个批次事件，这不是时间序列问题，是离散事件预测问题。正确方向：批次事件三层预测架构（When → What → How Much）。"

然而 main.py 的模型列表仍然在扩展——从最初的 CatBoost/LSTM 到 VMD-Transformer-CatBoost、N-HiTS、CondCatBoost、TwoStage——全部是时间序列方法。这是一个根本性的方法论矛盾：项目团队已经得出"这不是时间序列问题"的结论，但工程实现仍在时间序列框架内不断叠加模型复杂度。

**第一性原理分析**: 如果数据的生成过程是离散批次事件（27个采购批次散布在74个月中），那么任何假设信号连续性的模型（VMD、LSTM、Transformer、SARIMA、N-HiTS）都在建模一个不存在的结构。这不是模型参数调不好的问题，而是模型假设与数据生成过程（Data Generating Process, DGP）根本不匹配。

**建议**:
1. 立即停止在 main.py 中新增时间序列模型。这些模型的边际收益已经为零（项目自己的实验报告已证实）。
2. 将 scripts/batch_event_model.py 的三层预测架构作为论文的核心方法，main.py 中的时间序列模型仅作为"对比基线"存在。
3. 论文的叙事应从"我们的模型更好"转变为"我们发现了为什么传统方法失败，并提出了正确的问题表述"。

---

#### 发现 #2: "时间序列预测"与"离散事件预测"的范式差异被忽视

**两种范式的根本区别**:

| 维度 | 时间序列范式 | 离散事件范式 |
|------|------------|------------|
| 信号假设 | 连续、平滑、可分解 | 离散、脉冲式、不可分解 |
| 零值含义 | 信号恰好经过零点 | 事件未发生（完全不同的信息） |
| 预测目标 | 下一时刻的信号值 | 事件何时发生 + 事件的内容 |
| 有效样本量 | 全部观测值（81个月） | 仅非零事件（27个批次） |
| 外部因子作用 | 影响信号幅值 | 影响事件概率 |
| 适用模型 | ARIMA/LSTM/Transformer | 生存分析/点过程/分类+回归 |

当前数据：74个月中35-38%的月份需求为零。CV > 1。零值不是"信号很小"，而是"本月没有采购批次"。VMD分解出的IMF在零值处通过互相抵消来还原零（报告中已验证 corr_with_signal = 0.27~0.53），这恰好证明VMD在拟合一个不存在的连续结构。

**建议**: 论文应以"电力物资月度需求的生成过程是离散批次事件而非连续时间序列"为核心论点。这是一个方法论贡献（纠正了错误的问题表述），而不是又一个模型改进。

---

### 二、模型选型的方法论问题

#### 发现 #3: 模型复杂度与有效样本量严重不匹配

信息论的基本约束：要从数据中学到 p 个参数的有效模型，至少需要 O(p) 个独立信息量（有效样本）。

| 模型 | 参数估计 | 有效训练样本 | 参数/样本比 | 结论 |
|------|---------|------------|-----------|------|
| CatBoost | ~500 | ~45 (非零月) | 11:1 | 严重过拟合 |
| VMD-CatBoost | ~500 + VMD | ~45 | 11:1 | 严重过拟合 |
| Transformer-LSTM | ~800 | ~33 窗口 | 24:1 | 极端过拟合 |
| N-HiTS | ~2000+ | ~33 | 60:1 | 不可能学习 |
| TwoStage | ~1000 | ~45+27 | 10:1 | 严重过拟合 |
| SARIMA | ~10 | ~69 | 1:7 | 唯一样本充足的模型 |
| NaiveSeasonal | 0 | N/A | N/A | 无过拟合风险 |

实验报告已证实：零参数模型（NaiveSeasonal、批次事件模型）与 500-1000 参数模型表现相当。这不是巧合，而是信息论定律：从15-27个非零月中不可能学到500个参数的任何有效映射。

**建议**:
1. 如果继续保留时间序列模型作为对比基线，应选择参数量远小于有效样本量的模型（如 SARIMA、ETS、线性回归）。
2. 在论文中明确报告每个模型的参数量与有效样本量之比，作为"为什么复杂模型失败"的定量解释。
3. 移除 Transformer、N-HiTS 等深度学习模型——它们在81个月度数据点上没有理论基础。

---

#### 发现 #4: VMD 分解在间歇性需求数据上缺乏理论支撑

VMD（变分模态分解）的设计假设是：信号可以被分解为 K 个具有不同中心频率的带限本征模态函数（IMF），每个 IMF 是振幅/频率调制的连续信号。

对于间歇性需求数据（35%+零值），这个假设不成立：
- 零值不携带频率信息。VMD 被迫用多个 IMF 的叠加抵消来还原零值（IMF1 + IMF2 + IMF3 ≈ 0），这不是"分解"，而是"拟合噪声"。
- 非零值之间没有平滑过渡。从一个月的 0 到下个月的 100 再到下个月的 0，这种脉冲式变化无法用有限带宽的 IMF 表示。
- VMD 的 K 值优化基于频率比阈值（1.5），但对于本质上是非平稳脉冲序列的数据，频率比没有物理意义。

**建议**: 在论文中保留 VMD 相关实验作为"分解方法在间歇性需求上的失败案例"，而不是作为模型架构的一部分。这反而能强化"这不是时间序列问题"的论点。

---

#### 发现 #5: 模型对比缺乏间歇性需求领域的标准方法

当前模型列表：CatBoost、NaiveSeasonal、Persistence、SARIMA、CondCatBoost、N-HiTS、TwoStage。

缺失的间歇性需求专用方法（学术界公认的标准方法）：
- **Croston (1972)**: 分别建模需求间隔和需求量，是间歇性需求预测的开创性工作
- **SBA (Syntetos-Boylan Approximation, 2005)**: Croston 的修正版，消除偏差
- **TSB (Teunter-Syntetos-Babai, 2011)**: 用需求概率替代需求间隔，对过时报废品更准确
- **ADIDA (Aggregate-Disaggregate Intermittent Demand Approach)**: 时间聚合后预测再分解
- **Zero-inflated models**: 零膨胀泊松/负二项回归

实验报告中提到了 Croston-TSB-LightGBM，但它出现在 scripts/run_new_models.py 中，不在 main.py 的主流水线里。这意味着论文主体实验缺乏与间歇性需求领域标准方法的对比。

**建议**: 将 Croston/SBA/TSB 纳入 main.py 的核心模型列表。如果它们也失败（如实验报告暗示），这进一步强化了"这不是间歇性需求预测问题，而是离散事件预测问题"的论点。

---

### 三、实验设计的结构性缺陷

#### 发现 #6: 单一 train/test 分割 + 12 个测试点 = 统计上无意义的评估

当前评估：前 69 个月训练，后 12 个月测试。12 个测试点中约 4 个为零值（35%零值率），实际只有约 8 个非零预测点。

统计影响：
- R² 对单个极端值极度敏感。在 12 个测试点上，一个异常好的预测可以将 R² 从 -0.5 提升到 0.3，一个异常差的预测可以将 R² 从 0.3 降到 -1.0。
- 8 个非零点无法区分模型 A 和模型 B 的差异是否具有统计显著性。
- 没有置信区间、没有标准差、没有 p-value——当前报告的 R² 差异可能在噪声范围内。

**第一性原理分析**: 一个 12 个测试点的评估，等价于做一次实验就下结论。在统计学上，至少需要 30 个独立测试样本才能得到可靠的均值估计。在只有 12 个测试点的情况下，任何模型排序都不具有统计可靠性。

**建议**:
1. 采用滚动窗口回测（rolling window backtesting）：以 6 个月为步长，从第 48 个月开始，每次向后移动窗口做预测。可以得到约 5-6 个评估窗口的平均指标。
2. 报告每个窗口的指标 ± 标准差，而不是单一窗口的指标。
3. 使用 Diebold-Mariano 检验或 Wilcoxon signed-rank 检验来判断模型间差异是否统计显著。

---

#### 发现 #7: 基线模型选择不够严格

当前基线：NaiveSeasonal（去年同期值）、Persistence（上月值）。

缺失的关键基线：
- **Naive-Mean (历史均值)**: 实验报告中有，main.py 中没有
- **线性回归 / Ridge / Lasso**: 最基础的参数化模型，参数量极少
- **Random (随机预测)**: 作为"不可能更差"的下界
- **Expert judgment / 规则系统**: 实际业务中使用的预测方式

更重要的是，基线的实现本身也有 Bug（Bug #5: NaiveSeasonal/Persistence 在 log 空间反归一化缺少 expm1）。基线如果不可信，那么所有"超越基线"的结论也不可信。

**建议**: 修复基线 Bug 后，重新评估所有模型是否真正超越零参数基线。如果连 NaiveSeasonal 都无法稳定超越（实验报告已暗示），那么问题不在于模型选择，而在于问题定义。

---

#### 发现 #8: 消融实验设计不清晰

论文需要证明的核心命题是："VMD分解 + CatBoost融合" 这个架构的各组件是否都有贡献。

当前消融路径（基于 CLAUDE.md 描述的旧架构）：
1. CatBoost (baseline)
2. VMD-CatBoost (加VMD)
3. VMD-LSTM-CatBoost (加LSTM)
4. VMD-LSTM直接求和 (去掉CatBoost融合)
5. VMD-SVR (换掉CatBoost)

问题：
- 消融路径在 main.py 中已不存在（LSTM 被替换为 Transformer，SVR 被移除）。当前的消融逻辑断裂。
- 没有"仅用 lag/rolling 特征、不用 VMD"的消融对照，无法区分 VMD 分解和特征工程各自的贡献。
- 没有"仅用 project_count 单因子"的消融对照，无法量化 project_count 一枝独秀的影响。

**建议**: 重新设计消融实验，确保每个组件（VMD、Transformer、两阶段策略、特征工程）都有对应的"移除该组件"对照。

---

### 四、文档一致性与项目管理问题

#### 发现 #9: CLAUDE.md 描述的项目与实际代码严重脱节

CLAUDE.md 描述的项目状态 vs 当前代码实际状态：

| 维度 | CLAUDE.md 描述 | 实际代码 |
|------|---------------|---------|
| 物资数量 | 3种（电缆、变压器、避雷器） | 5种（避雷器、互感器、绝缘子、断路器、电抗器） |
| 模型列表 | 5个（CatBoost, VMD-CatBoost, VMD-LSTM-CatBoost, VMD-LSTM, VMD-SVR） | 7个（CatBoost, NaiveSeasonal, Persistence, SARIMA, CondCatBoost, NHiTS, TwoStage） |
| LSTM架构 | MultiFeatureLSTM(5→12) + SingleFeatureLSTM(1→8) | 已被 Transformer 替代 |
| 影响因子 | 7个（load_growth, investment, history_demand, equipment_cost, typhoon, lightning, rainstorm） | 6个（project_count, transformer_bids, monthly_bid_count, uhv_bids, has_batch, digital_bids） |
| 训练/测试分割 | 24个月/12个月 | 69个月/12个月 |
| 序列窗口 | SEQ_LEN=6, stride=1 | SEQ_LEN=12, stride=1 |
| 数据源 | 模拟数据（36个月） | 真实ECP数据（81个月） |
| 参考文献 | ITERATIONS.md（30轮优化记录） | 文件不存在 |

CLAUDE.md 描述的是一个完全不同的项目阶段（早期3物资36月模拟数据实验），而当前代码已经演进到5物资81月真实数据。文档完全失去了对项目的描述能力。

**建议**: 重写 CLAUDE.md 以反映当前项目状态。或者，考虑到 CLAUDE.md 第102行已承认方法论不可行，更好的做法是：将 CLAUDE.md 改为"项目演进记录"，保留旧内容作为历史参考，同时添加当前阶段的准确描述。

---

#### 发现 #10: 数据生成要求文档与当前数据不匹配

`inputs/数据生成要求和影响因素说明.txt` 描述了3种物资的模拟数据生成规则：
- 10kV电缆（0-25之间，4个因子）
- 柱上变压器（5-15套，4个因子）
- 10kV交流避雷器（双峰模式，4-6月和7-9月）

当前项目使用的5种物资（交流避雷器、电容式电压互感器、交流支柱绝缘子、断路器保护、电抗器保护）来自真实ECP采购数据，不是模拟数据。该文档已经过时。

但该文档仍然有方法论价值：它记录了原始设计中"影响因素按重要性排序"的思路。对比当前数据，原始设计中的"统一因子"（负荷增长量、雷击）在当前数据中对应的是 transformer_bids、monthly_bid_count 等全局因子——它们对所有物资相同的设计与原始"统一"概念一脉相承，但这种设计本身就有问题（见 AUDIT_REPORT.md Issue #6）。

**建议**: 将该文档归档为 `inputs/历史文档/`，并编写新的数据说明文档，描述当前5种物资的数据来源（ECP数据库）、时间范围（2020-05~2026-06）、以及6个因子的含义和数据来源。

---

#### 发现 #11: 关键文档缺失

CLAUDE.md 中引用但实际不存在的文件：
- `ITERATIONS.md` — 30轮优化记录（CLAUDE.md L96 引用）
- `README.md` — 项目说明文件
- `bidding-ecp-data/reports/预测失败根因分析与改进方案.md` — 根因分析报告（CLAUDE.md L102 引用）

其中 `bidding-ecp-data/` 是另一个项目（上游数据源），但该报告是否可访问需要验证。如果不可访问，CLAUDE.md L102 的结论就缺乏可追溯的证据链。

**建议**: 在项目中保存一份 `预测失败根因分析与改进方案.md` 的副本（如果上游文件可访问），或在当前项目中重新生成该分析。

---

### 五、数据管道设计问题

#### 发现 #12: 两套并行的数据源和代码路径造成混乱

当前项目存在两套并行的实现：

| 维度 | main.py（主流水线） | scripts/batch_event_model.py |
|------|-------------------|----------------------------|
| 数据源 | inputs/data.xlsx (5 sheets, 81 months) | bidding-ecp-data/data/ecp_data.db + inputs/data/real_material_data.xlsx |
| 物资 | 5种（避雷器、互感器、绝缘子、断路器、电抗器） | 5种（控制电缆、电缆保护管、通信单元、电缆接线端子、蝶式绝缘子） |
| 时间范围 | 未知（81个月） | 2020-05 ~ 2026-06 (74个月) |
| 训练/测试 | 69/12 | 62/12 |
| 方法 | 时间序列模型（7个） | 三层批次事件模型 |

两套代码使用了完全不同的5种物资。main.py 用的是 `data.xlsx` 中的"交流避雷器"等5种物资，batch_event_model.py 用的是 `real_material_data.xlsx` 中的"控制电缆"等5种物资。这两组实验结果无法直接对比，因为评估的是不同物资。

**第一性原理分析**: 如果论文要论证"时间序列方法失败，批次事件方法更好"，需要在相同物资、相同数据上的公平对比。当前的两套系统无法提供这样的对比。

**建议**:
1. 统一数据源：选择一组物资（建议用真实ECP数据），让 main.py 和 batch_event_model.py 在完全相同的数据上运行。
2. 统一评估流程：使用同一个 evaluate_model() 函数、同一套 train/test 分割。
3. 在同一张表格中展示所有模型的指标，确保对比的公平性。

---

#### 发现 #13: 特征工程的因果逻辑不清晰

当前6个因子：project_count（项目数量）、transformer_bids（变压器招标）、monthly_bid_count（月度招标数）、uhv_bids（特高压招标）、has_batch（是否有批次）、digital_bids（数字化招标）。

方法论问题：
- **因果方向不明确**: project_count 与需求量高度相关（r > 0.86），但因果方向是什么？是"项目多 → 需求多"还是"需求多 → 项目多"？如果是后者（project_count 本身就是需求量的另一种度量），那么使用它做预测就是数据泄露——用需求本身来预测需求。
- **5个全局因子缺乏区分度**: transformer_bids、monthly_bid_count、uhv_bids、has_batch、digital_bids 对所有物资完全相同。这意味着它们编码的是"市场整体活跃度"，而不是"特定物资的驱动因素"。在单一物资的时间序列预测中，这些全局因子等价于时间特征（因为它们对所有物资相同，唯一的变化维度就是时间）。
- **缺少滞后因子**: 真实的采购决策通常有提前期。当月的 project_count 可能影响的是2-3个月后的需求量。当前代码没有探索因子的最优滞后阶数。

**建议**:
1. 明确 project_count 的定义：如果它是"已批准的工程项目数"（领先指标），则是合理的预测因子；如果它是"物资采购项目数"（同步/滞后指标），则存在因果泄露。
2. 为每个因子计算不同滞后期（lag-1, lag-3, lag-6）的 Spearman 相关性，找到最优滞后结构。
3. 如果5个全局因子确实只编码"市场整体活跃度"，考虑用 PCA 降维为1个全局因子，避免共线性。

---

### 六、论文叙事与学术贡献定位

#### 发现 #14: 论文的核心贡献定位尚不清晰

从项目文档中可以看出三种可能的论文叙事方向，但它们互相矛盾：

**方向A（原始方向）**: "VMD + CatBoost/LSTM 的混合模型在配电网物资预测中的优越性"
- 问题：实验报告已证明这些模型无法超越 NaiveSeasonal 基线。方向A已被证伪。

**方向B（实验报告方向）**: "系统实验证明时间序列方法在间歇性需求上的结构性失败"
- 优势：有坚实的实验证据（7模型 × 5物资的全败）。
- 问题：负面结果（negative result）的论文在期刊发表中面临更高的拒稿风险。

**方向C（批次事件方向）**: "从时间序列到离散事件：电力物资需求预测的范式转换"
- 优势：方法论贡献明确（纠正了错误的问题表述），且有零参数的批次事件模型作为正面证据。
- 问题：批次事件模型本身还不够成熟（Layer 3 季节均值粗糙，层间独立性假设未验证）。

**建议**: 选择方向C作为论文的核心叙事。具体来说：
1. 用方向B的实验证据作为"为什么需要范式转换"的论据。
2. 用方向C的批次事件模型作为"正确问题表述下的解决方案"。
3. 论文标题建议："电力物资间歇性需求的生成机制：从时间序列预测到批次事件预测的范式转换"

---

#### 发现 #15: 实验报告与主流水线的脱节

`reports/模型对比实验日报表.md` 提供了非常有价值的分析，但它引用的实验（Croston-TSB-LightGBM、LGB Direct Multi-Step、批次事件三层模型）全部在 scripts/ 目录中，不在 main.py 中。这意味着：

- main.py 的运行结果与报告中的数据不一致。
- 报告中提到的"27个批次事件"、"74个月"等数据特征与 main.py 的"81个月"不一致（可能使用了不同的数据文件）。
- 报告中的结论（"批次事件模型以零训练参数在3/5物资上胜出"）无法从 main.py 的输出中复现。

**建议**: 将 scripts/ 中的实验成果（特别是批次事件模型和 Croston/TSB 方法）整合到 main.py 中，使得一次运行就能生成论文所需的全部对比结果。

---

### 七、改进路线图（按优先级排序）

#### 第一优先级: 战略决策（立即）

| 序号 | 行动项 | 决策内容 |
|------|--------|---------|
| S1 | 确定论文核心叙事 | 选择"范式转换"方向：时间序列→离散事件 |
| S2 | 统一数据集 | 选择一组物资和一个数据源，让所有模型在相同数据上评估 |
| S3 | 决定 main.py 的角色 | 作为论文实验的主流水线？还是仅作为历史代码归档？ |

#### 第二优先级: 基础修复（1-2天）

| 序号 | 行动项 | 具体操作 |
|------|--------|---------|
| F1 | 修复代码级 Bug | 执行 AUDIT_REPORT.md Phase 1（expm1, roll3_te, SARIMA） |
| F2 | 统一评估流程 | 确保所有模型（包括 baseline）使用相同的 inverse_transform 管道 |
| F3 | 添加 Croston/SBA/TSB | 将间歇性需求标准方法纳入主流水线 |

#### 第三优先级: 实验重设计（3-5天）

| 序号 | 行动项 | 具体操作 |
|------|--------|---------|
| E1 | 实现滚动窗口回测 | 至少 3 个评估窗口，报告均值 ± 标准差 |
| E2 | 添加统计显著性检验 | Diebold-Mariano 或 Wilcoxon signed-rank |
| E3 | 完整消融实验 | 每个组件都有"移除对照" |
| E4 | 统一批次事件模型 | 将 batch_event_model.py 整合到主流水线 |

#### 第四优先级: 文档与可复现性（1-2天）

| 序号 | 行动项 | 具体操作 |
|------|--------|---------|
| D1 | 重写 CLAUDE.md | 反映当前项目状态和战略方向 |
| D2 | 编写数据说明文档 | 描述物资来源、因子定义、数据质量 |
| D3 | 归档过时文档 | 将数据生成要求.txt 移入历史文档目录 |
| D4 | 补全缺失文档 | 复制或重建 ITERATIONS.md 和根因分析报告 |

---

### 八、结论

本次设计层面审计发现了 **15 个方法论/设计层面的问题**，可归纳为三个层次：

**层次一：问题定义错误（发现 #1-#2）**
项目已经自行发现"这不是时间序列问题"，但工程实现仍在此方向上投入。这是最根本的矛盾——如果问题定义错了，无论模型多精巧、参数多精细，都不可能得到有意义的结果。

**层次二：实验方法论不严谨（发现 #3-#8）**
即使作为时间序列预测实验，当前的模型选型（参数量远超有效样本量）、基线选择（缺少间歇性需求标准方法）、评估设计（12个测试点无交叉验证）都不满足发表级别的实验标准。

**层次三：项目管理与文档一致性（发现 #9-#15）**
CLAUDE.md 与实际代码严重脱节、两套并行的数据和代码路径、关键文档缺失——这些问题虽然不直接影响模型性能，但会严重影响论文的可信度和可复现性。

**核心建议**: 停止在时间序列方向上的投入，将项目重心转向批次事件预测范式。这不仅是项目自己实验得出的结论，也是数据本身的特性所决定的。论文的核心贡献应定位为"揭示了电力物资需求的离散事件本质，并通过系统实验证明传统时序方法的结构性失败"。
