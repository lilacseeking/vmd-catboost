# 配电网物资需求预测系统 — 设计与方法论审计报告（第二轮复审）

审计日期: 2026-07-08 (首次) → 2026-07-08 (第二轮复审)
审计范围: 项目全部文档（CLAUDE.md、数据生成要求、实验报告）+ main.py 1759行 + scripts/ 子模块
审计方法: 第一性原理逐层质疑，从问题定义到实验设计到文档一致性
与 AUDIT_REPORT.md 的关系: 该文件聚焦代码级 Bug，本报告聚焦设计层面和方法论层面的结构性问题

---

### 〇、修复状态总览

| 发现编号 | 问题 | 状态 | 变化 |
|---------|------|------|------|
| #1 | 项目承认方法不可行但继续投入 | ❌ 未解决 | — |
| #2 | 时间序列 vs 离散事件范式差异 | ⚠️ 部分缓解 | VMD 循环论证已禁用，体现方法论觉醒 |
| #3 | 模型复杂度 vs 有效样本量 | ⚠️ 部分缓解 | VMD/Transformer 禁用；TwoStage 统一框架参数更少 |
| #4 | VMD 在间歇性需求上无理论支撑 | ✅ 已解决 | VMD 模型已禁用（循环论证） |
| #5 | 缺少间歇性需求标准方法对比 | ⚠️ 部分解决 | Croston-SBA 已加入主流水线；SBA/TSB 仍缺失 |
| #6 | 单一 train/test + 12 测试点 | ❌ 未解决 | — |
| #7 | 基线模型不够严格 | ⚠️ 部分解决 | Croston-SBA 加入；Naive-Mean/Random 仍缺失 |
| #8 | 消融实验不清晰 | ⚠️ 部分缓解 | VMD 消融被禁用，当前消融路径更简洁 |
| #9 | CLAUDE.md 与代码严重脱节 | ❌ 未解决 | 代码大幅变化但文档未更新 |
| #10 | 数据生成要求文档不匹配 | ❌ 未解决 | — |
| #11 | 关键文档缺失 | ❌ 未解决 | — |
| #12 | 两套并行数据源/代码路径 | ❌ 未解决 | — |
| #13 | 特征工程因果逻辑不清晰 | ⚠️ 部分缓解 | 移除 has_batch/digital_bids；但因果方向仍未澄清 |
| #14 | 论文核心贡献定位不清晰 | ❌ 未解决 | — |
| #15 | 实验报告与主流水线脱节 | ⚠️ 部分缓解 | Croston-SBA 已整合；批次事件模型仍独立 |
| 新#16 | run_catboost 双重定义导致消融链断裂 | 🆕 新发现 | — |
| 新#17 | VMD 禁用后论文叙事需要重新定位 | 🆕 新发现 | — |

---

### 一、根本性问题（未解决）

#### 发现 #1: 项目文档已明确承认当前方法不可行，但项目仍在此方向上持续投入 — ❌ 未解决

**证据**: CLAUDE.md 第 102 行仍然明确写道：

> "当前方法已确认不可行...这不是时间序列问题，是离散事件预测问题。正确方向：批次事件三层预测架构（When → What → How Much）。"

**当前 main.py 模型列表**（仍在时间序列框架内）：CatBoost(TwoStage)、NaiveSeasonal、Persistence、SARIMA、CondCatBoost(TwoStage)、N-HiTS、Croston-SBA、LightGBM(TwoStage)、TwoStage。共 9 个模型，其中 8 个是时间序列/回归方法。

**正面信号**: VMD 模型的禁用（L1714: `# [DISABLED] 循环论证`）表明项目已经开始质疑时间序列方向的有效性。但尚未完全接受"离散事件"范式——批次事件模型（scripts/batch_event_model.py）仍未整合到主流水线。

**建议**: 不变——停止时间序列方向投入，将批次事件模型作为论文核心方法。

---

#### 发现 #2: "时间序列预测"与"离散事件预测"的范式差异 — ⚠️ 部分缓解

**缓解**: VMD 禁用体现了对"连续性假设"的质疑。TwoStage 框架（先分类是否有需求）本质上是离散事件思维的第一步——先判断"事件是否发生"，再预测"事件规模"。

**残留**: TwoStage 框架的 Stage 2 仍然是连续回归，未采用离散事件专用方法（如点过程、生存分析）。论文叙事中仍未明确将"离散事件预测"定位为核心范式。

---

### 二、模型选型方法论

#### 发现 #3: 模型复杂度与有效样本量 — ⚠️ 部分缓解

**缓解**: VMD 分解模型（参数量最大、循环论证风险最高的部分）已被禁用。当前主模型 CatBoost/TwoStage/LightGBM 的参数量虽然仍远超有效样本量（~45 非零月），但树模型有内置正则化（early_stopping + l2_leaf_reg），比 Transformer/N-HiTS 更不容易过拟合。

**残留**: 仍无参数量与有效样本量的定量对比。论文中应报告这一信息。

#### 发现 #4: VMD 分解在间歇性需求上缺乏理论支撑 — ✅ 已解决

**解决方式**: VMD 模型在 main.py L1714-1715 被明确禁用，注释指出"循环论证: VMD分解y->IMF作特征->预测y, Sigma(IMF)~=y"。这是方法论层面的正确判断——VMD 将 y 分解为 IMF 分量再用 IMF 预测 y，存在结构性循环论证。

#### 发现 #5: 缺少间歇性需求标准方法 — ⚠️ 部分解决

**改善**: Croston-SBA 已加入主流水线（L1441-1466），`run_croston_sba()` 实现了 SBA 修正版。这是间歇性需求领域的标准方法之一。

**残留**: SBA 的更优版本（Syntetos-Boylan 2005 修正）和 TSB（Teunter-Syntetos-Babai 2011）仍未加入。对于论文来说，仅有 Croston-SBA 已经是一个有意义的对比基线，但加入 TSB 会更完整。

---

### 三、实验设计

#### 发现 #6: 单一 train/test 分割 + 12 个测试点 — ❌ 未解决

**现状不变**: main.py 仍使用前 69 月训练、后 12 月测试的单一分割。12 个测试点中约 4 个为零值。

**建议不变**: 实现滚动窗口回测（rolling window backtesting），至少 3 个评估窗口。

#### 发现 #7: 基线模型选择 — ⚠️ 部分解决

**改善**: Croston-SBA 加入后，基线列表更完整：
- NaiveSeasonal（季节性朴素） ✅
- Persistence（持久性） ✅
- SARIMA ✅
- Croston-SBA ✅ (新增)

**残留缺失**:
- Naive-Mean（历史均值）——实验报告中有但 main.py 中没有
- Random/Zero 预测——作为不可能更差的下界

#### 发现 #8: 消融实验设计 — ⚠️ 部分缓解

**改善**: VMD 模型禁用后，消融路径变得更清晰：
1. CatBoost (实际是 TwoStage-CatBoost)
2. CondCatBoost (更深 TwoStage)
3. LightGBM (TwoStage + LightGBM)
4. TwoStage (独立实现的两阶段)

**新发现的问题**: run_catboost 被二次定义（详见 AUDIT_REPORT.md Bug #6），导致消融链中缺少真正的"朴素 CatBoost 基线"。当前 "CatBoost" 标签实际上是 TwoStage-CatBoost，与 "TwoStage" 标签高度重叠。

---

### 四、文档一致性（未解决）

#### 发现 #9: CLAUDE.md 与代码严重脱节 — ❌ 未解决

CLAUDE.md 自上次审计以来**未做任何更新**，仍描述：

| 维度 | CLAUDE.md 描述 | 实际代码 |
|------|---------------|---------|
| 物资 | 3种（电缆、变压器、避雷器） | 5种（中文命名） |
| 模型 | 5个（含 LSTM） | 9个（含 Transformer、Croston-SBA、LightGBM） |
| 因子 | 7个（load_growth, investment...） | 4个（project_count, transformer_bids...） |
| 数据 | 36个月模拟数据 | 81个月真实ECP数据 |
| 分割 | 24/12 | 69/12 |
| 行号 | 1315行 | 1759行 |
| LSTM | MultiFeatureLSTM + SingleFeatureLSTM | 已替换为 Transformer |
| ITERATIONS.md | 引用存在 | 文件不存在 |
| README.md | 项目核心文件 | 文件不存在 |

**影响**: CLAUDE.md 完全失去对项目的描述能力。任何基于此文档的后续开发都会产生错误。

#### 发现 #10: 数据生成要求文档不匹配 — ❌ 未解决

`inputs/数据生成要求和影响因素说明.txt` 仍描述原始 3 种物资的模拟数据生成规则，与当前 5 种物资的真实 ECP 数据不匹配。

#### 发现 #11: 关键文档缺失 — ❌ 未解决

- `ITERATIONS.md` — 仍不存在
- `README.md` — 仍不存在
- `bidding-ecp-data/reports/预测失败根因分析与改进方案.md` — 未验证可访问性

---

### 五、数据管道

#### 发现 #12: 两套并行数据源和代码路径 — ❌ 未解决

| 维度 | main.py | scripts/batch_event_model.py |
|------|---------|------------------------------|
| 数据源 | inputs/data.xlsx (5 sheets, 81月) | bidding-ecp-data + real_material_data.xlsx (74月) |
| 物资 | 交流避雷器等 5 种 | 控制电缆等 5 种 |
| 方法 | 时间序列模型 | 三层批次事件模型 |
| 评估 | evaluate_model() | 独立评估代码 |

两套系统使用不同物资、不同数据、不同评估流程，无法直接公平对比。

#### 发现 #13: 特征工程因果逻辑 — ⚠️ 部分缓解

**改善**: 移除了 `has_batch` 和 `digital_bids`（弱因子）。当前 4 个因子中 3 个全局相同，仅 `project_count` 有区分度。

**残留**: `project_count` 的因果方向仍未澄清——是"项目多→需求多"（领先指标）还是"需求多→项目多"（同步指标）？如果是后者，使用它预测就是因果泄露。

---

### 六、论文叙事

#### 发现 #14: 论文核心贡献定位 — ❌ 未解决

三种可能的叙事方向仍然互相矛盾：
- **方向 A**（VMD+CatBoost 优越性）：已被自己的实验证伪，VMD 已禁用
- **方向 B**（系统实验揭示时序方法失败）：有实验证据，但是负面结果
- **方向 C**（范式转换：时序→离散事件）：方法论贡献最明确，但批次事件模型未整合

**建议不变**: 选择方向 C，将批次事件模型整合到主流水线，在相同数据上做公平对比。

---

### 七、新发现

#### 发现 #16: run_catboost 双重定义导致消融链断裂 🆕

main.py L767 和 L1353 两次定义 `run_catboost`，后者（TwoStage 版本）覆盖前者（直接回归版本）。这意味着：

1. 论文消融链缺少一个真正的"朴素 CatBoost"对比点
2. 汇总表中 "CatBoost" 和 "TwoStage" 实际是两个高度重叠的 TwoStage 实现
3. 无法量化 TwoStage 框架本身（vs 直接回归）的贡献

**建议**: 恢复一个非 TwoStage 的朴素 CatBoost 基线，使消融链完整：朴素 CatBoost → TwoStage-CatBoost → CondCatBoost → LightGBM → TwoStage（独立实现）。

#### 发现 #17: VMD 禁用后论文叙事需要重新定位 🆕

VMD 模型曾是论文的架构核心（"VMD-CatBoost"是项目名称和项目名），现在被禁用并标注为"循环论证"。这意味着：

1. 项目名称 "vmd-catboost" 不再准确
2. 论文需要重新定位核心创新点
3. VMD 相关代码（约 400 行）成为死代码

**建议**: 在论文中将 VMD 实验定位为"方法论探索中的失败案例"——"我们尝试了 VMD 分解方法，发现其在间歇性需求数据上存在循环论证问题，因此放弃"。这种"什么不可行"的叙事在学术论文中同样有价值。

---

### 八、改进路线图（按优先级排序）

#### 第一优先级: 战略决策（立即）

| 序号 | 行动项 | 状态 |
|------|--------|------|
| S1 | 确定论文核心叙事（方向 C: 范式转换） | ❌ 未执行 |
| S2 | 统一数据集（main.py 和 batch_event_model 使用相同物资和数据） | ❌ 未执行 |
| S3 | 将批次事件模型整合到 main.py | ❌ 未执行 |

#### 第二优先级: 代码级修复（1-2天）

| 序号 | 行动项 | 状态 |
|------|--------|------|
| F1 | 修复 run_catboost 双重定义（恢复朴素基线） | ❌ 未执行 |
| F2 | 修复 VMD/TF 超参字典键名 | ❌ 未执行 |
| F3 | N-HiTS 阈值调整 | ❌ 未执行 |
| F4 | 添加 Naive-Mean 基线 | ❌ 未执行 |

#### 第三优先级: 实验重设计（3-5天）

| 序号 | 行动项 | 状态 |
|------|--------|------|
| E1 | 实现滚动窗口回测 | ❌ 未执行 |
| E2 | 添加统计显著性检验 | ❌ 未执行 |
| E3 | 完整消融实验（含朴素 CatBoost 基线） | ❌ 未执行 |
| E4 | 添加 TSB 方法 | ❌ 未执行 |

#### 第四优先级: 文档与可复现性（1-2天）

| 序号 | 行动项 | 状态 |
|------|--------|------|
| D1 | 重写 CLAUDE.md | ❌ 未执行（最高优先级的文档任务） |
| D2 | 编写数据说明文档 | ❌ 未执行 |
| D3 | 归档过时文档 | ❌ 未执行 |
| D4 | 补全缺失文档（README.md, ITERATIONS.md） | ❌ 未执行 |

---

### 九、结论

**15 个原始发现中 1 个已解决（VMD 循环论证）、5 个部分缓解、9 个未解决。新发现 2 个问题。**

代码层面有实质性改善（expm1 架构修复、roll3_te 泄露修复、VMD 禁用、Croston-SBA 加入、统一 TwoStage），但设计层面和方法论层面的根本性矛盾——**时间序列范式 vs 离散事件范式**——仍然未解决。

项目的核心战略决策仍然是：继续在当前时间序列框架内修补，还是接受自己实验数据的结论，转向批次事件预测范式？这个问题的答案将决定论文的叙事方向和工程投入的回报率。
